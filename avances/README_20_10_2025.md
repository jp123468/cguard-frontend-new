# 🗓️ Avances del Día — 20 de Octubre de 2025

## 👮 Guardias de Seguridad (Listado)
- Se corrigió el nombre del componente a `SecurityGuardsPage` y se reconstruyó la UI.
- Búsqueda en tiempo real por nombre, correo y teléfono.
- Panel de filtros con categorías relevantes (Cliente, Puesto de seguridad, Conjunto de Habilidades, Departamento y Estado).
- Estados visibles mediante `Badge` (Activo, Pendiente, Invitado).
- Selección individual y selección total por página; acciones por fila con menú.
- Paginación eficiente con selector de elementos (10/25/50) y controles anterior/siguiente.
- Optimización con `useMemo` y `useCallback` para evitar cálculos y recreaciones innecesarias.
- Render de filas basado en `paginatedGuards` para respetar la paginación.

## ➕ Nuevo Guardia (Flujos de incorporación)
- Implementación de página `NewSecurityGuardPage` con 4 pestañas:
  - Invitar (Correo/SMS) con bloques dinámicos (añadir/eliminar).
  - Unirse por código: OTP de 6 dígitos, copiar al portapapeles y regenerar código.
  - Invitar por enlace: manejo de múltiples teléfonos.
  - Crear perfil: formulario completo (nombre, correo, móvil, contraseña, cliente y sitio).
- Formularios con `react-hook-form` + `zod` (`security-guard.schema`, `join-by-code.schema`, `invite-by-link.schema`, `create-profile.schema`).
- Componentes reutilizables: `FormBlock`, `GuardTabsHeader`, `Combobox`, `SubmitBar`, `FormErrorSummary`.
- `toast` para feedback de usuario; validaciones en `onTouched` y estados de envío.

## 💬 Mensajero
- Componente `Messenger` con layout de 2 columnas:
  - Sidebar con pestañas Bandeja/Archivados, búsqueda y listado de conversaciones.
  - Badges: "Puesto de seguridad" y "Chat individual".
  - Botón "Añadir Nuevo" con menú para "Nuevo Mensaje" y "Nuevo Grupo".
- Modales:
  - `NewMessageDialog`: búsqueda y selección de guardias/sitios para iniciar conversación.
  - `NewGroupDialog`: nombre de grupo, selección de sitio (opcional) y elección de guardias.
- Panel derecho con header de acciones (Detalles, Unidireccional, Archivar) y `Composer` para enviar mensajes.
- Props para conectar API: `onSelectConversation`, `onCreateMessage`, `onCreateGroup`, `onToggleOneWay`, `onArchive`, `onOpenDetails`.
- Página `MessengerPage` integrada con `Breadcrumb` y `AppLayout`.

## 📂 Archivos Relevantes del Día
- `src/pages/admin/security-guards/SecurityGuardsPage.tsx` (actualizado y optimizado).
- `src/pages/admin/security-guards/NewSecurityGuardPage.tsx` (implementado con formularios y tabs).
- `src/pages/admin/messenger/Messenger.tsx` (UI y modales del mensajero).
- `src/pages/admin/messenger/MessengerPage.tsx` (integración en layout y breadcrumb).

## ✅ Verificación
- La UI de Guardias respeta paginación, búsqueda y selección por página.
- El Mensajero presenta estructura completa (sidebar, modales, composer) lista para conectar a servicios.
- No se alteraron rutas ni layouts globales; mantenimiento del estilo del proyecto.

## ▶️ Próximos Pasos
- Conectar ambos módulos a servicios/API (listado/paginación del servidor, creación, archivado, etc.).
- Añadir estados de carga, vacíos y manejo de errores.
- Incorporar permisos/roles para acciones sensibles.
- Agregar pruebas unitarias/integración para filtrado, paginación y flujos de formularios.