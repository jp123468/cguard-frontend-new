
Frameworks principales:
- **React + TypeScript**
- **TailwindCSS**
- **React Router**
- **Lucide React** (iconos)
- **Sonner** (notificaciones)

---

## 🧩 Layout Principal (AppLayout)

### ✅ Cambios realizados
- Sidebar **fijo** en desktop (`top-0 h-screen`).
- Drawer móvil con **overlay** semitransparente.
- El `Header` se sincroniza en tamaño (`h-14`) y en desplazamiento (`lg:ml-64`).
- Bloqueo del scroll en móvil cuando el menú está abierto.
- Sin cortes visuales: sidebar llega **desde el tope hasta el fondo**.

### 💡 Comportamiento responsive
- **Desktop (≥ lg)**:
  - Sidebar visible (`fixed left-0 top-0 h-screen`).
  - Header y Main se desplazan (`lg:ml-64`) cuando está abierto.
- **Móvil (< lg)**:
  - Sidebar se transforma en un drawer con `translate-x`.
  - Overlay cubre el fondo (`bg-black/30`).
  - Se bloquea el scroll (`overflow: hidden` en body).

---

## 🧱 Header

### Funcionalidades
- Botón **hamburguesa** funcional en móvil y desktop.
- Iconos de **mensajes**, **búsqueda**, **ayuda**, **notificaciones**.
- Menú desplegable de usuario con:
  - Información (nombre, correo)
  - Navegación rápida: Configuración, Suscripción, Registros, Historial.
  - Submenú de tema (modo claro / oscuro)
  - Botón **Cerrar sesión** con `useAuth().signOut()`

### Estructura
- Altura fija: `h-14`
- Sticky y con z-index superior (`z-50`)
- Fondo blanco y borde inferior (`border-b border-gray-200`)

---

## 📚 Sidebar

### Funcionalidad principal
- Menú principal con íconos y submenús dinámicos.
- Soporte para **NavLink** (activos resaltados).
- Color activo: gradiente `from-[#FFF0F0] to-[#FFEAEA]` con borde lateral `#F36A6D`.
- Submenús expandibles con íconos rotativos (`ChevronRight`).
- Logo superior “GuardsPro” con diseño idéntico al original.

### Mejora clave
- Sidebar ahora ocupa toda la altura (`top-0 h-screen`) para mantener alineación con el header.

---

## 🔐 Autenticación

### 1. Login
- Campos: `email`, `password`.
- Validación básica y conexión con `AuthContext`.
- Redirección a `/dashboard` al éxito.
- Enlaces:
  - “¿Olvidaste tu contraseña?” → `/forgot-password`
  - “Registrarse” → `/register`

### 2. Register
- Campos: `firstName`, `lastName`, `email`, `password`.
- Registro con `AuthContext.register()`.
- Notificación de éxito/error con **Sonner**.
- Redirección automática según flujo de negocio.

### 3. Forgot Password
- Campo único: `email`.
- Llama a `AuthContext.sendPasswordReset()`.
- Muestra toast de confirmación de correo enviado.

---

## ⚙️ AuthContext

Provee los métodos principales:
```ts
{
  user,
  signIn(email, password),
  register(data),
  sendPasswordReset(email),
  signOut()
}
